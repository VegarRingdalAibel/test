"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getPath_1 = require("./getPath");
const logger_1 = require("./logger");
function printSettings(options) {
    // configuration name
    logger_1.Logger.info(`Typechecker settings - name:`, `<dim>${options.name}</dim>`);
    // base path being used
    logger_1.Logger.info(`Typechecker settings - basepath:`, `<dim>${options.basePath}</dim>`);
    // get tsconfig path and options
    if (options.tsConfig) {
        let tsconf = getPath_1.getPath(options.tsConfig, options);
        logger_1.Logger.info(`Typechecker settings - tsconfig:`, `<dim>${tsconf}</dim>`);
    }
    else {
        logger_1.Logger.info(`Typechecker settings - tsconfig:`, `<dim>undefined, using ts defaults/override if defined</dim>`);
    }
}
exports.printSettings = printSettings;
//# sourceMappingURL=printSettings.js.map